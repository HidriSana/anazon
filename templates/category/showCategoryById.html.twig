{% extends 'base.html.twig' %}

{% block title %}Catégorie:
	{{category.name}}
{% endblock %}

{% block body %}
	<div>
		<div>
			<h1>Catégorie:
				{{category.name}}</h1>
			<a href="{{path('edit_category', {id: category.id})}}" class="btn btn-success">Modifier</a>
		</div>
	</div>
{% endblock %}
